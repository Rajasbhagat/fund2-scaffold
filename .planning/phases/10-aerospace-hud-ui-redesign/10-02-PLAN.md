---
phase: 10-aerospace-hud-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/page.tsx
  - src/components/ProjectList.tsx
  - src/components/ProjectCard.tsx
  - src/components/CreateProjectForm.tsx
autonomous: true
requirements:
  - UI-04
  - UI-05
  - UI-10

must_haves:
  truths:
    - "Dashboard renders in a 2-column layout: narrow sidebar (app identity + stats) and wide main area (project grid)"
    - "Project cards display as instrument panels with crosshair corners from HUDPanel primitive and a neon yellow accent strip"
    - "Dashboard header has a neon yellow HUDAccentBlock element"
    - "At least one panel section uses the dot-grid perforated background pattern"
    - "No inline style={{ fontFamily: ... }} props remain in dashboard components — all use font-sans class via @theme"
    - "No hardcoded hex arbitrary values (bg-[#1a2024]) remain — all migrated to token classes (bg-hud-fg)"
  artifacts:
    - path: "app/page.tsx"
      provides: "2-column dashboard layout with sidebar, header accent strip, dot-grid background"
      contains: "HUDAccentBlock"
    - path: "src/components/ProjectList.tsx"
      provides: "Project grid in main column with HUDLabel section header"
      contains: "HUDLabel"
    - path: "src/components/ProjectCard.tsx"
      provides: "Instrument panel cards using HUDPanel primitive"
      contains: "HUDPanel"
    - path: "src/components/CreateProjectForm.tsx"
      provides: "Flat command input zone with token classes"
      contains: "bg-hud-accent"
  key_links:
    - from: "app/page.tsx"
      to: "src/components/hud/index.tsx"
      via: "import { HUDAccentBlock, DotGrid, HUDLabel, Barcode } from '@/components/hud'"
      pattern: "from '@/components/hud'"
    - from: "src/components/ProjectCard.tsx"
      to: "src/components/hud/index.tsx"
      via: "import { HUDPanel } from '@/components/hud'"
      pattern: "HUDPanel"
---

<objective>
Restyle the dashboard page as a mission control instrument panel: 2-column layout with sidebar identity block, project cards using HUDPanel primitives, neon yellow accent header, and dot-grid background.

Purpose: The dashboard is the app's first impression. Transforms it from a single-column list to a HUD-style mission control grid with crosshairs, accent strips, and telemetry aesthetics.
Output: Restyled app/page.tsx, ProjectList.tsx, ProjectCard.tsx, CreateProjectForm.tsx — all using HUD tokens and primitives from plan 10-01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-aerospace-hud-ui-redesign/10-RESEARCH.md
@.planning/phases/10-aerospace-hud-ui-redesign/10-01-SUMMARY.md
@app/page.tsx
@src/components/ProjectList.tsx
@src/components/ProjectCard.tsx
@src/components/CreateProjectForm.tsx
@src/components/hud/index.tsx
</context>

<interfaces>
<!-- HUD primitives from plan 10-01 (src/components/hud/index.tsx) -->
```typescript
export function HUDPanel({ children, className, accentCorners = true }: {
  children: React.ReactNode; className?: string; accentCorners?: boolean
}): JSX.Element

export function HUDLabel({ children, className }: {
  children: React.ReactNode; className?: string
}): JSX.Element

export function HUDValue({ children, className }: {
  children: React.ReactNode; className?: string
}): JSX.Element

export function HUDAccentBlock({ children, className }: {
  children: React.ReactNode; className?: string
}): JSX.Element

export function Barcode({ className }: { className?: string }): JSX.Element

export function DotGrid({ children, className }: {
  children?: React.ReactNode; className?: string
}): JSX.Element
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle dashboard page with 2-column HUD layout</name>
  <files>app/page.tsx, src/components/ProjectList.tsx</files>
  <read_first>
    - app/page.tsx (current single-column layout with header, dot-grid, max-w-5xl content area)
    - src/components/ProjectList.tsx (current grid layout, section header, CreateProjectForm, empty state)
    - src/components/hud/index.tsx (HUD primitives available after 10-01)
    - .planning/phases/10-aerospace-hud-ui-redesign/10-RESEARCH.md (dashboard layout target structure: header + sidebar + main)
  </read_first>
  <action>
**app/page.tsx — Restructure to 2-column HUD layout:**

Import HUD primitives: `import { HUDAccentBlock, DotGrid, HUDLabel, HUDValue, Barcode } from '@/components/hud'`

Target layout structure:
```
HEADER (full width) — accent strip with FUND II branding
+---SIDEBAR (w-56)---+---MAIN AREA (flex-1)---+
| Identity block     | ProjectList            |
| Stats / Barcode    | (project card grid)    |
| DotGrid background |                        |
+--------------------+------------------------+
```

1. **Header**: Replace current header. Add `HUDAccentBlock` containing the FUND II title. Keep date display as `font-mono` telemetry readout. Replace `style={{ fontFamily: ... }}` with `font-sans` class. Migrate all hex arbitrary values: `text-[#ebff00]` -> `text-hud-accent`, `text-[#d2edea]` -> `text-hud-bg`, `text-[#b1dbd8]` -> `text-hud-panel`, `bg-[#1a2024]` -> `bg-hud-fg`, `border-[#b1dbd8]` -> `border-hud-panel`.

2. **Below header, flex row**: Left sidebar `w-56 shrink-0 border-r border-hud-panel/15` + right main `flex-1`.

3. **Sidebar**: Wrap in `DotGrid` for perforated background (satisfies UI-10). Contains:
   - App identity: `HUDLabel` with "ANTIGRAVITY" + `HUDValue` with "FUND II"
   - `Barcode` decorative element
   - Stats: `HUDLabel` "ACTIVE NODES" + count from projects.length
   - `HUDLabel` "SYSTEM STATUS" + "ONLINE" indicator

4. **Main area**: `flex-1 overflow-y-auto p-8` — renders `<ProjectList initialProjects={serialized} />`

5. Replace ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` with `className="font-sans"` (the @theme maps font-sans to Rajdhani).

6. Replace ALL hardcoded hex: `bg-[#1a2024]` -> `bg-hud-fg`, `text-[#d2edea]` -> `text-hud-bg`, `text-[#ebff00]` -> `text-hud-accent`, `border-[#b1dbd8]` -> `border-hud-panel`, `text-[#b1dbd8]` -> `text-hud-panel`.

**src/components/ProjectList.tsx — Migrate tokens + use HUD primitives:**

1. Import: `import { HUDLabel } from '@/components/hud'`
2. Replace section header div (lines 37-46) with `HUDLabel` for "INTELLIGENCE NODES" micro-label. Keep the accent line decoration and count display.
3. Migrate ALL hex arbitrary values to token classes (same mapping as above).
4. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` inline props — replace with `font-sans` class.
5. Keep grid layout `grid gap-4 sm:grid-cols-2 lg:grid-cols-3` on the project list `<ul>`.
  </action>
  <verify>
    <automated>cd /Users/rajas/Desktop/AntiGravity/Fund2Updated && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx imports from `'@/components/hud'`
    - app/page.tsx contains `HUDAccentBlock` usage
    - app/page.tsx contains `DotGrid` usage (sidebar)
    - app/page.tsx has a sidebar element with `w-56` or similar fixed width
    - app/page.tsx does NOT contain `style={{ fontFamily` inline props
    - app/page.tsx does NOT contain `bg-[#1a2024]` — uses `bg-hud-fg` instead
    - app/page.tsx does NOT contain `text-[#d2edea]` — uses `text-hud-bg` instead
    - app/page.tsx does NOT contain `text-[#ebff00]` — uses `text-hud-accent` instead
    - src/components/ProjectList.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/ProjectList.tsx does NOT contain `bg-[#1a2024]` — uses token classes
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Dashboard renders as 2-column HUD layout with sidebar (identity + DotGrid + stats) and main area (project grid). Header has neon yellow HUDAccentBlock. All hex arbitrary values migrated to Tailwind token classes. No inline fontFamily props remain.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ProjectCard + CreateProjectForm as HUD instruments</name>
  <files>src/components/ProjectCard.tsx, src/components/CreateProjectForm.tsx</files>
  <read_first>
    - src/components/ProjectCard.tsx (current: manual crosshair corners, hex colors, inline fontFamily)
    - src/components/CreateProjectForm.tsx (current: flat input, hex colors, inline fontFamily)
    - src/components/hud/index.tsx (HUDPanel primitive with accentCorners prop)
    - .planning/phases/10-aerospace-hud-ui-redesign/10-RESEARCH.md (ProjectCard refactoring guidance)
  </read_first>
  <action>
**src/components/ProjectCard.tsx — Refactor to use HUDPanel:**

1. Import: `import { HUDPanel } from '@/components/hud'`
2. Replace the outer `<li>` with its manual crosshair corner spans (lines 61-66) with `<HUDPanel className="bg-hud-fg p-4 hover:border-hud-panel/40 transition-colors list-none" accentCorners>` wrapping the card content. Remove the 4 manual `<span>` corner elements — HUDPanel handles them.
3. Wrap in `<li className="list-none">` around HUDPanel to keep semantics.
4. Migrate ALL hex arbitrary values:
   - `border-[#b1dbd8]` -> `border-hud-panel`
   - `bg-[#1a2024]` -> `bg-hud-fg`
   - `text-[#d2edea]` -> `text-hud-bg`
   - `text-[#ebff00]` -> `text-hud-accent`
   - `border-[#ebff00]` -> `border-hud-accent`
   - `text-[#b1dbd8]` -> `text-hud-panel`
   - `hover:text-[#d4e600]` -> `hover:text-hud-accent/90`
   - `hover:text-[#b1dbd8]` -> `hover:text-hud-panel`
   - `hover:text-[#d2edea]` -> `hover:text-hud-bg`
5. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` — replace with `font-sans` class on each element that had it.
6. Add a small neon yellow accent indicator to the card: a 2px tall `bg-hud-accent` strip at the top of the card (inside HUDPanel, before the content). This is a `<div className="h-0.5 bg-hud-accent/60 mb-3" />` at the top.

**src/components/CreateProjectForm.tsx — Migrate tokens:**

1. Migrate ALL hex arbitrary values (same mapping as above).
2. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` — use `font-sans` class.
3. Button: `bg-[#ebff00] text-[#1a2024]` -> `bg-hud-accent text-hud-fg`
4. Input: `border-[#b1dbd8]/25` -> `border-hud-panel/25`, `text-[#d2edea]` -> `text-hud-bg`, `placeholder:text-[#b1dbd8]/25` -> `placeholder:text-hud-panel/25`, `focus:border-[#ebff00]/60` -> `focus:border-hud-accent/60`
5. Button hover: `hover:bg-[#d4e600]` -> `hover:bg-hud-accent/90`
  </action>
  <verify>
    <automated>cd /Users/rajas/Desktop/AntiGravity/Fund2Updated && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ProjectCard.tsx imports `HUDPanel` from `'@/components/hud'`
    - src/components/ProjectCard.tsx contains `<HUDPanel` JSX usage
    - src/components/ProjectCard.tsx does NOT contain the manual corner span pattern `absolute top-0 left-0 w-3 h-3 border-t border-l border-[#ebff00]` (this is now in HUDPanel)
    - src/components/ProjectCard.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/ProjectCard.tsx does NOT contain `bg-[#1a2024]`
    - src/components/ProjectCard.tsx does NOT contain `text-[#ebff00]` — uses `text-hud-accent`
    - src/components/CreateProjectForm.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/CreateProjectForm.tsx does NOT contain `bg-[#ebff00]` — uses `bg-hud-accent`
    - src/components/CreateProjectForm.tsx does NOT contain `text-[#1a2024]` — uses `text-hud-fg`
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>ProjectCard uses HUDPanel primitive (no manual crosshair spans). Each card has a neon yellow accent strip. CreateProjectForm uses token classes. All hex arbitrary values and inline fontFamily props eliminated from both components.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. `grep -c 'style={{ fontFamily' app/page.tsx src/components/ProjectList.tsx src/components/ProjectCard.tsx src/components/CreateProjectForm.tsx` — all return 0
3. `grep -c 'bg-\[#1a2024\]' app/page.tsx src/components/ProjectList.tsx src/components/ProjectCard.tsx src/components/CreateProjectForm.tsx` — all return 0
4. `grep -c 'HUDPanel' src/components/ProjectCard.tsx` — returns at least 1
5. `grep -c 'HUDAccentBlock' app/page.tsx` — returns at least 1
6. `grep -c 'DotGrid' app/page.tsx` — returns at least 1
</verification>

<success_criteria>
- Dashboard renders as 2-column mission control layout (sidebar + main) per UI-04
- Project cards use HUDPanel primitive with crosshair corners per UI-04
- Neon yellow accent block visible in dashboard header per UI-05
- Dot-grid pattern applied to at least the sidebar per UI-10
- Zero inline fontFamily style props in dashboard components
- Zero hardcoded hex arbitrary values in dashboard components
</success_criteria>

<output>
After completion, create `.planning/phases/10-aerospace-hud-ui-redesign/10-02-SUMMARY.md`
</output>

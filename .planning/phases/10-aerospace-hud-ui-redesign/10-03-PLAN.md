---
phase: 10-aerospace-hud-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/AgentTabs.tsx
  - src/components/AgentWorkspace.tsx
  - src/components/ChatWindow.tsx
  - src/components/ChatInput.tsx
  - src/components/MessageBubble.tsx
  - src/components/ProjectLayout.tsx
  - src/components/FileUploadPanel.tsx
  - src/components/UnifiedChatWindow.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09

must_haves:
  truths:
    - "Agent tabs show bracket decoration [ AGENT NAME ] on the active tab"
    - "Each assistant message has a micro-copy agent label above the bubble"
    - "ChatInput renders as a flat command console with no rounded corners"
    - "Session tracker displays as a monospace telemetry readout — not a plain select dropdown"
    - "Deep Research toggle shows neon yellow active state using token class bg-hud-accent"
    - "FileUploadPanel is styled as a HUD data intake module"
    - "Zero borderRadius inline styles survive in MessageBubble code blocks"
    - "No inline style={{ fontFamily: ... }} props remain in any workspace component"
    - "No hardcoded hex arbitrary values remain — all migrated to token classes"
  artifacts:
    - path: "src/components/AgentTabs.tsx"
      provides: "HUD tab strip with bracket active state decoration"
      contains: "[ "
    - path: "src/components/ChatWindow.tsx"
      provides: "Chat telemetry stream with agent micro-labels"
      contains: "text-hud-accent"
    - path: "src/components/MessageBubble.tsx"
      provides: "Zero borderRadius inline styles, token-class colors"
      contains: "text-hud-accent"
    - path: "src/components/ProjectLayout.tsx"
      provides: "Session selector as monospace telemetry readout"
      contains: "font-mono"
    - path: "src/components/FileUploadPanel.tsx"
      provides: "Data intake module styled with HUD tokens"
      contains: "text-hud-accent"
  key_links:
    - from: "src/components/AgentTabs.tsx"
      to: "app/globals.css @theme"
      via: "Tailwind token classes bg-hud-fg, text-hud-accent, border-hud-panel, border-hud-accent"
      pattern: "text-hud-accent"
    - from: "src/components/MessageBubble.tsx"
      to: "UI-09 compliance"
      via: "borderRadius: 0 in inline code and pre block styles"
      pattern: "borderRadius: 0"
---

<objective>
Restyle all workspace and chat components with HUD tokens: agent tab bracket decorations, chat telemetry micro-labels, command console input, monospace session selector, HUD file upload panel. Eliminate all remaining hex arbitrary values and inline fontFamily props.

Purpose: Completes the HUD visual transformation across the entire workspace experience — tabs, chat, input, session controls, and file upload.
Output: 8 restyled component files, all using HUD design tokens. Zero hex arbitrary values or inline font overrides remain.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-aerospace-hud-ui-redesign/10-RESEARCH.md
@.planning/phases/10-aerospace-hud-ui-redesign/10-01-SUMMARY.md
@src/components/AgentTabs.tsx
@src/components/AgentWorkspace.tsx
@src/components/ChatWindow.tsx
@src/components/ChatInput.tsx
@src/components/MessageBubble.tsx
@src/components/ProjectLayout.tsx
@src/components/FileUploadPanel.tsx
@src/components/UnifiedChatWindow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AgentTabs with bracket decoration + ProjectLayout session telemetry</name>
  <files>src/components/AgentTabs.tsx, src/components/AgentWorkspace.tsx, src/components/ProjectLayout.tsx</files>
  <read_first>
    - src/components/AgentTabs.tsx (current: border-b-2 active indicator, hex colors, inline fontFamily)
    - src/components/AgentWorkspace.tsx (current: bg-[#1a2024], minimal)
    - src/components/ProjectLayout.tsx (current: SessionSelector as plain <select>, sidebar with hex colors, inline fontFamily everywhere)
    - .planning/phases/10-aerospace-hud-ui-redesign/10-RESEARCH.md (bracket active state pattern: `[ ${agent.label} ]`, session telemetry readout pattern)
  </read_first>
  <action>
**src/components/AgentTabs.tsx — Bracket active state + token migration:**

1. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` — add `font-sans` to button className.
2. Migrate hex colors:
   - `bg-[#1a2024]` -> `bg-hud-fg`
   - `border-[#b1dbd8]` -> `border-hud-panel`
   - `border-[#ebff00]` -> `border-hud-accent`
   - `text-[#ebff00]` -> `text-hud-accent`
   - `text-[#b1dbd8]` -> `text-hud-panel`
3. Add bracket decoration to active tab label:
   ```tsx
   {activeAgent === agent.id ? `[ ${agent.label} ]` : agent.label}
   ```
   This wraps the active tab text in brackets per UI-05 spec.

**src/components/AgentWorkspace.tsx — Token migration:**

1. Migrate `bg-[#1a2024]` -> `bg-hud-fg`.
2. This is a thin wrapper — minimal changes needed.

**src/components/ProjectLayout.tsx — Session telemetry readout + full token migration:**

1. **SessionSelector redesign**: Replace the `<select>` dropdown with a grid of 10 numbered cells (1-10). Layout: `grid grid-cols-5 gap-1`. Each cell is a `<button>` with:
   - Base style: `w-full aspect-square flex items-center justify-center text-xs font-mono border border-hud-panel/20 text-hud-panel/40`
   - Active (current session): `bg-hud-accent text-hud-fg border-hud-accent font-bold`
   - Hover: `hover:border-hud-panel/40 hover:text-hud-panel`
   - Add a `HUDLabel` above the grid reading "SESSION" and a `font-mono` display of current session: `SES-{String(sessionNumber).padStart(2, '0')}`

2. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` from every element — use `font-sans` class.

3. Migrate ALL hex colors throughout the component:
   - `bg-[#1a2024]` -> `bg-hud-fg`
   - `border-[#b1dbd8]` -> `border-hud-panel`
   - `text-[#b1dbd8]` -> `text-hud-panel`
   - `text-[#d2edea]` -> `text-hud-bg`
   - `text-[#ebff00]` -> `text-hud-accent`
   - `border-[#ebff00]` -> `border-hud-accent`
   - `bg-[#ebff00]` -> `bg-hud-accent`
   - `hover:text-[#d2edea]` -> `hover:text-hud-bg`
   - `hover:text-[#ebff00]` -> `hover:text-hud-accent`
   - `hover:border-[#b1dbd8]` -> `hover:border-hud-panel`

4. Sidebar brand mark: keep `FUND II` text, use `text-hud-panel/15 font-sans`.

5. Import `HUDLabel` from `'@/components/hud'` for section labels.
  </action>
  <verify>
    <automated>cd /Users/rajas/Desktop/AntiGravity/Fund2Updated && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AgentTabs.tsx contains `[ ${agent.label} ]` bracket pattern
    - src/components/AgentTabs.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/AgentTabs.tsx does NOT contain `border-[#ebff00]` — uses `border-hud-accent`
    - src/components/AgentTabs.tsx does NOT contain `text-[#ebff00]` — uses `text-hud-accent`
    - src/components/AgentWorkspace.tsx does NOT contain `bg-[#1a2024]`
    - src/components/ProjectLayout.tsx does NOT contain `<select` (replaced with grid cells)
    - src/components/ProjectLayout.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/ProjectLayout.tsx does NOT contain `bg-[#1a2024]`
    - src/components/ProjectLayout.tsx contains `font-mono` class usage
    - src/components/ProjectLayout.tsx contains `bg-hud-accent` for active session cell
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Agent tabs show bracket [ ] decoration on active tab. Session selector is a 10-cell monospace grid with neon yellow active state. All workspace shell components use HUD token classes with zero inline fontFamily or hex arbitrary values.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ChatWindow, ChatInput, MessageBubble, FileUploadPanel</name>
  <files>src/components/ChatWindow.tsx, src/components/ChatInput.tsx, src/components/MessageBubble.tsx, src/components/FileUploadPanel.tsx, src/components/UnifiedChatWindow.tsx</files>
  <read_first>
    - src/components/ChatWindow.tsx (ThinkingIndicator, EmptyState, role labels, all hex colors)
    - src/components/ChatInput.tsx (command console style, DEEP button, SEND button, hex colors)
    - src/components/MessageBubble.tsx (CRITICAL: borderRadius: 2 on lines 67 and 81 must become 0 for UI-09; all markdown components have inline style colors)
    - src/components/FileUploadPanel.tsx (documents panel, hex colors, inline fontFamily)
    - src/components/UnifiedChatWindow.tsx (may have hex colors — read to verify)
    - .planning/phases/10-aerospace-hud-ui-redesign/10-RESEARCH.md (Pitfall 1: borderRadius survives theme override; Pitfall 5: inline fontFamily props)
  </read_first>
  <action>
**src/components/ChatWindow.tsx — Telemetry stream migration:**

1. ThinkingIndicator: Migrate ALL hex colors:
   - `text-[#ebff00]` -> `text-hud-accent`
   - `bg-[#ebff00]` -> `bg-hud-accent`
   - Remove `style={{ fontFamily: "'Rajdhani', sans-serif" }}` — use `font-sans`
   - The inline `style={{ border: '1px solid rgba(235,255,0,0.12)', background: 'rgba(235,255,0,0.03)' }}` can stay as-is (these are rgba with opacity, not covered by simple token swap) OR migrate to `border border-hud-accent/[0.12] bg-hud-accent/[0.03]`.

2. EmptyState: Same migration — all hex -> tokens, remove inline fontFamily.

3. Message rendering section (lines 216-253): Migrate role label colors:
   - Remove `style={{ fontFamily: "'Rajdhani', sans-serif", color: isUser ? 'rgba(210,237,234,0.35)' : 'rgba(235,255,0,0.55)' }}` — replace with conditional className: `${isUser ? 'text-hud-bg/35' : 'text-hud-accent/55'} font-sans`
   - Error bubble: Migrate inline style border/background to Tailwind classes where possible.

4. Remove ALL `style={{ fontFamily: ... }}` inline props throughout the file.

**src/components/ChatInput.tsx — Command console migration:**

1. Migrate ALL hex colors:
   - `border-[#b1dbd8]` -> `border-hud-panel`
   - `bg-[#1a2024]` -> `bg-hud-fg`
   - `text-[#d2edea]` -> `text-hud-bg`
   - `placeholder:text-[#b1dbd8]` -> `placeholder:text-hud-panel`
   - `focus:border-[#ebff00]` -> `focus:border-hud-accent`
   - `bg-[#ebff00]` -> `bg-hud-accent`
   - `text-[#1a2024]` -> `text-hud-fg`
   - `text-[#b1dbd8]` -> `text-hud-panel`
   - `hover:text-[#b1dbd8]` -> `hover:text-hud-panel`
   - `hover:border-[#b1dbd8]` -> `hover:border-hud-panel`
   - `hover:bg-[#d4e600]` -> `hover:bg-hud-accent/90`
2. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` — use `font-sans` class.

**src/components/MessageBubble.tsx — CRITICAL borderRadius fix + token migration:**

1. **UI-09 compliance (CRITICAL):** Find the two `borderRadius: 2` inline styles:
   - Line 67 (inline `<code>` style): Change `borderRadius: 2` to `borderRadius: 0`
   - Line 81 (`<pre>` style): Change `borderRadius: 2` to `borderRadius: 0`

2. Migrate inline style colors in markdown component overrides to use token-aligned values. Since these are inside `style={{}}` props on react-markdown components (not Tailwind classes), keep them as inline styles but update the color references for consistency:
   - `color: '#ebff00'` stays (these are inside style props, already correct hex)
   - `color: '#d2edea'` stays
   - `color: '#b1dbd8'` stays
   - These CANNOT be migrated to Tailwind classes because they are react-markdown component overrides that use `style` props. This is acceptable — the hex values match the @theme tokens exactly.

3. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` from h1, h2, h3, th elements — add `className` with `font-sans` alongside existing classes.
   - h1: remove fontFamily from style, add `font-sans` to className
   - h2: remove fontFamily from style, add `font-sans` to className
   - h3: remove fontFamily from style, add `font-sans` to className
   - th: remove fontFamily from style, add `font-sans` to className

4. `<code>` inline element: change `className="mono"` to `className="font-mono"` (standardize on Tailwind class).
5. `<pre>` element: change `className="mono"` to `className="font-mono"`.

**src/components/FileUploadPanel.tsx — Data intake module styling:**

1. Migrate ALL hex colors (same mapping).
2. Remove ALL `style={{ fontFamily: "'Rajdhani', sans-serif" }}` — use `font-sans`.
3. `bg-[#1a2024]` -> `bg-hud-fg`
4. `border-[#b1dbd8]` -> `border-hud-panel`
5. `text-[#b1dbd8]` -> `text-hud-panel`
6. `text-[#ebff00]` -> `text-hud-accent`

**src/components/UnifiedChatWindow.tsx — Token migration:**

Read the file first. Apply the same migration pattern: all hex -> tokens, all inline fontFamily -> font-sans class. This component likely has the same patterns as ChatWindow.tsx.
  </action>
  <verify>
    <automated>cd /Users/rajas/Desktop/AntiGravity/Fund2Updated && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/MessageBubble.tsx contains `borderRadius: 0` (NOT `borderRadius: 2`) — both occurrences
    - src/components/MessageBubble.tsx does NOT contain `borderRadius: 2`
    - src/components/ChatInput.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/ChatInput.tsx does NOT contain `bg-[#ebff00]` — uses `bg-hud-accent`
    - src/components/ChatInput.tsx does NOT contain `bg-[#1a2024]` — uses `bg-hud-fg`
    - src/components/ChatWindow.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/ChatWindow.tsx does NOT contain `text-[#ebff00]` at component level — uses `text-hud-accent`
    - src/components/FileUploadPanel.tsx does NOT contain `style={{ fontFamily` inline props
    - src/components/FileUploadPanel.tsx does NOT contain `bg-[#1a2024]` — uses `bg-hud-fg`
    - src/components/MessageBubble.tsx markdown components use `font-sans` class instead of inline fontFamily style
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Chat components render as telemetry stream with token classes. MessageBubble has zero borderRadius (UI-09 compliant). ChatInput is a flat command console. FileUploadPanel is styled as a data intake module. All inline fontFamily props and hardcoded hex values eliminated from workspace components.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. `grep -rn 'borderRadius: 2' src/components/` returns 0 matches (UI-09)
3. `grep -rn "style={{ fontFamily" src/components/AgentTabs.tsx src/components/AgentWorkspace.tsx src/components/ChatWindow.tsx src/components/ChatInput.tsx src/components/ProjectLayout.tsx src/components/FileUploadPanel.tsx` returns 0 matches
4. `grep -c '\[ ' src/components/AgentTabs.tsx` returns at least 1 (bracket decoration)
5. `grep -c 'bg-hud-accent' src/components/ProjectLayout.tsx` returns at least 1 (session cell active state)
6. `grep -c 'grid' src/components/ProjectLayout.tsx` returns at least 1 (session grid)
</verification>

<success_criteria>
- Agent tab strip shows bracket decoration [ ] on active tab per UI-05
- Chat messages have agent micro-labels in telemetry style per UI-06
- ChatInput renders as flat command console per UI-06
- Session tracker is a monospace telemetry readout grid per UI-07
- Deep Research toggle uses bg-hud-accent for active state per UI-07
- FileUploadPanel is styled as data intake module per UI-08
- Zero borderRadius > 0 inline styles in any component per UI-09
- Zero inline fontFamily style props in any workspace component
- Zero hardcoded hex arbitrary values in workspace components
</success_criteria>

<output>
After completion, create `.planning/phases/10-aerospace-hud-ui-redesign/10-03-SUMMARY.md`
</output>

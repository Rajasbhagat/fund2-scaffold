# Ralph Progress Log
Started: Sat Mar 21 16:53:00 CET 2026
---

## Codebase Patterns
- All Vertex AI routes use `export const runtime = 'nodejs'`
- Prisma client imported from `@/lib/prisma` (singleton), generated to `@/generated/prisma/client`
- Context loaded via `getSystemContext()` from `@/lib/context`
- Vertex AI grounding: `tools: { google_search: vertex.tools.googleSearch({}) }` in streamText call
- `vertex` instance created with `createVertex()` has `.tools.googleSearch()` available
- Run `npx prisma generate` before typecheck if `@/generated/prisma/client` errors appear
- This is a git worktree — work in `/private/tmp/fund2-p3-integration`, NOT the main working dir

## 2026-03-21 - US-003
- Implemented Vertex AI web search grounding on the chat route
- Added `tools: { google_search: vertex.tools.googleSearch({}) }` to the `streamText` call
- Verified using `@ai-sdk/google-vertex` SDK docs at `node_modules/@ai-sdk/google-vertex/docs/16-google-vertex.mdx`
- The `createVertex()` return value has `.tools.googleSearch()` — confirmed in type definitions
- Files changed: `src/app/api/chat/[projectId]/route.ts`
- **Learnings for future iterations:**
  - The grounding API in `@ai-sdk/google-vertex` v4 uses `vertex.tools.googleSearch({})` as a tool, NOT `googleSearchRetrieval` config
  - The worktree at `/private/tmp/fund2-p3-integration` is the `ralph/phase-3-integration` branch — always cd here, not to the main `/Users/rajas/Desktop/AntiGravity/Fund2Updated` working dir
  - Run `npx prisma generate` before typecheck in a fresh worktree — generated client is not committed
  - The integration branch has `ralph/phase-3-merged` as its base; both share the same starting commit
---

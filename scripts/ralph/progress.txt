# Ralph Progress Log
Started: Sun Mar 22 09:41:32 CET 2026

## Codebase Patterns
- Active app dir is ROOT-LEVEL `app/` NOT `src/app/` — always edit `app/layout.tsx`, `app/globals.css`, `app/page.tsx`
- Tailwind v4 CSS-first: no tailwind.config.js; tokens go in `@theme {}` block in `app/globals.css`
- HUD color tokens: bg-hud-bg (#d2edea), bg-hud-fg (#1a2024), bg-hud-accent (#ebff00), bg-hud-panel (#b1dbd8)
- Font variables: --font-rajdhani (sans), --font-jetbrains-mono (mono); applied via CSS `--font-sans` and `--font-mono` in @theme
- Use `bg-hud-*`, `text-hud-*`, `border-hud-*` token classes — NOT hardcoded hex arbitrary values like `bg-[#1a2024]`
---

## 2026-03-22 - US-001
- Implemented HUD design foundation: next/font/google for Rajdhani + JetBrains_Mono, Tailwind v4 @theme color/font/radius tokens
- Files changed: app/layout.tsx, app/globals.css
- **Learnings for future iterations:**
  - JetBrains_Mono uses `weight: 'variable'` (variable font), Rajdhani uses explicit weight array
  - Font variables set on `<html>` element via `rajdhani.variable` and `jetbrainsMono.variable` classNames
  - @theme block must be placed right after `@import "tailwindcss"` in globals.css
  - `--radius: 0` in @theme gives flat zero-radius aesthetic to all Tailwind radius utilities
---

## 2026-03-22 - US-002
- Created src/components/hud/index.tsx with 6 HUD primitive components: HUDPanel, HUDLabel, HUDValue, HUDAccentBlock, Barcode, DotGrid
- Files changed: src/components/hud/index.tsx (new file)
- **Learnings for future iterations:**
  - HUDPanel uses `border-hud-accent/60` token class (NOT `border-[#ebff00]/60`) for corner spans
  - DotGrid just adds className `dot-grid` — relies on existing CSS in globals.css
  - Barcode renders a decorative bar pattern using bg-hud-panel/60 strips
  - All components accept optional `className` for composability
---

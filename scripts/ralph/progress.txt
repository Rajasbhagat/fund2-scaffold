# Ralph Progress Log
Started: Sat Mar 21 17:02:14 CET 2026
---

## 2026-03-21 - US-001
- Installed `react-markdown@^10.1.0` and `remark-gfm@^4.0.1` via npm
- Created `src/components/MessageBubble.tsx` as a Client Component
- User messages render as plain text with right-aligned blue bubble
- Model messages render through ReactMarkdown with prose-sm styling, left-aligned gray bubble
- `npx tsc --noEmit` shows no errors in MessageBubble (pre-existing errors in route.ts, page.tsx, prisma.ts are unrelated)
- Files changed: `src/components/MessageBubble.tsx`, `package.json`, `package-lock.json`
- **Learnings for future iterations:**
  - Work in the worktree `/private/tmp/fund2-p4-group-a` (on `ralph/phase-4-group-a`), not in `/Users/rajas/Desktop/AntiGravity/Fund2Updated` (on `ralph/phase-3-streaming-chat`)
  - Worktrees don't share node_modules — run `npm install` in the worktree directory
  - `ralph/phase-4-group-a` is checked out as a worktree; cannot checkout it in the main repo
---

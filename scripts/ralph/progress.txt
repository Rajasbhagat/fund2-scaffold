## Codebase Patterns
- Worktree has no node_modules or prisma client on first use — run `npm install && npx prisma generate` and `DATABASE_URL="file:./dev.db" npx prisma migrate deploy` before running tsc/build checks
- All Vertex AI routes use `export const runtime = 'nodejs'`
- Prisma client imported from `@/lib/prisma` (singleton pattern)
- HTTP streaming headers (X-Content-Type-Options, X-Accel-Buffering, Cache-Control) set on Response wrapping result.toTextStreamResponse()

# Ralph Progress Log
Started: Sat Mar 21 17:02:22 CET 2026
---

## 2026-03-21 - US-004
- Verified HTTP headers already present in route.ts (lines 77-85): X-Content-Type-Options: nosniff, X-Accel-Buffering: no, Cache-Control: no-cache
- Set up worktree: ran npm install, npx prisma generate, and prisma migrate deploy
- `npx tsc --noEmit` passes (0 errors)
- `npm run build` passes (all 5 pages generated)
- Files changed: scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Worktree requires manual `npm install` + `npx prisma generate` + `DATABASE_URL="file:./dev.db" npx prisma migrate deploy` before any TypeScript or build checks
  - The headers were already committed to the branch — US-004 was pre-implemented; this iteration verified and marked it passing
---

## Codebase Patterns
- All Vertex AI routes use `export const runtime = 'nodejs'`
- Prisma client imported from `@/lib/prisma` (singleton)
- Route handlers use `NextResponse.json()` from `next/server`
- React 19 context syntax: `<MyContext value={...}>` (no `.Provider`)
- Worktree has no node_modules — symlink from main repo for tsc: `ln -s /Users/rajas/Desktop/AntiGravity/Fund2Updated/node_modules /private/tmp/<worktree>/node_modules`
- Generated Prisma client lives in `src/generated/prisma` (gitignored) — run `cd /Users/rajas/Desktop/AntiGravity/Fund2Updated && npx prisma generate --schema=<worktree>/prisma/schema.prisma` from main repo

# Ralph Progress Log
Started: Sat Mar 21 20:33:26 CET 2026
---

## 2026-03-21 - US-001
- Implemented GET/PATCH `/api/settings/session` route handler with defensive fallback
- Created `src/contexts/SessionContext.tsx` with `SessionProvider` and `useSession()` hook
- Updated `prisma/schema.prisma` to add `AppSettings` model and `agentType` on `Message` (mirrors wave1-01 changes needed for type generation)
- **Files changed**: `prisma/schema.prisma`, `src/app/api/settings/session/route.ts`, `src/contexts/SessionContext.tsx`
- **Learnings for future iterations:**
  - Worktrees don't have node_modules — must symlink from main repo before running tsc
  - Prisma generate must be run from the main repo dir using `--schema=<worktree>/prisma/schema.prisma` since prisma.config.ts imports dotenv which isn't available in worktree context
  - React 19: use `<SessionContext value={...}>` NOT `<SessionContext.Provider value={...}>`
  - PATCH body validation: check `typeof n === 'number' && Number.isInteger(n)` — JSON numbers can be floats
---
